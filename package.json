{
  "name": "persevere-dsh-plugins",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Persevere with DSH plugin collection. Monorepo workspace for the DSH (DeepSeek Harness) plugins maintained by the Persevere with DSH project.",
  "license": "MIT",
  "author": "Xilong Liu",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/persevere-dsh/plugins.git"
  },
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b tsconfig.host.json",
    "codegen": "node scripts/gen-typert.mjs",
    "build": "npm run typecheck && npm run codegen && node scripts/run-workspaces.mjs build",
    "test": "node scripts/run-workspaces.mjs test",
    "clean": "node scripts/gen-typert.mjs --clean"
  },
  "devDependencies": {
    "@deepseek-ai/dsh-typert-generator": "0.1.5-rc.2",
    "@types/node": "^24.0.0",
    "tsdown": "^0.22.2",
    "typescript": "6.0.3"
  }
}
